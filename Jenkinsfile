pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                echo "build"
            }
        }

        stage("test") {
            steps {
                echo "testing the application"
            }
        }

        stage("deploy") {
            steps {
                echo "deploy"
            }
        }
    }
}
